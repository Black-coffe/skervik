# Consolidated learnings

**Format:** one entry per line (topic · date of last evidence · summary). Drop ephemeral state; keep project-specific friction and expensive lessons. Cap: 40 entries. Newest evidence wins — update, don't duplicate.

- **Lore tone locked to realism≈1900 + one Verne anomaly (2026-07-03).** Owner feedback: first draft gave "dosed mysticism" (mysterious Mist, draug-raider, Norse sagas/kitsch register). Correct frame: steam+sail era ≈1900, telegraph/insurance/markets (no ghosts/curses/monsters); Mist is unexplained natural anomaly only; Norse toponymy (Norway 1900 heritage) not "Viking age". Bind all future lore text, art briefs, naming, UI copy against this frame. Materialized in lore-primer §Зафиксировано владельцем (2026-07-03).
- **Trilingual RU/UA/EN from first playable build (2026-07-03).** Owner directive (ADR-0008): i18n framework + all three locales ship in M1/S1.6.6, not M4. Every user-facing term born in three languages (lore-primer glossary pattern); PR gate: string in fewer than 3 locales → reject. Expect 25–35% layout expansion for RU/UA. No text baked into art; fonts cover Cyrillic (ґєії). Internal docs stay RU, code EN.
- **Queen writes specs only, not code (2026-07-03).** Owner directive: top-model role = plans/docs/orchestration only; all code (even one-liners) via worker-code subagents (Sonnet by default, Opus for Tier 4). Token economy: top-model reserved for planning + final review. Bookend pattern: avoid mid-work code edits by Queen. Enforced in CLAUDE.vulyk.md + M1 plan §0.
- **Chrome throttles rAF in unfocused windows, breaking automation (2026-07-03).** Symptom: Pixi Ticker shows "10 FPS" even at `visibilityState==='visible'` (clamped to `maxElapsedMS=100`). Fix: synthetic CPU-cost bench (cycle renderer.render() under DevTools 4×/6× throttle), not real FPS. Perf handle exposed as `globalThis.__SKERVIK_PROTO__` (scene.ts). Screenshots: use `renderer.extract.base64()` (offscreen), not window.capture.
