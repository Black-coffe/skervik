# E2.8 — Interactive human play (client input) — EPIC PLAN

**Status:** PROPOSED (2026-07-14, awaiting owner approval before build) · **Tier:** 3
(client-heavy, cross-story) · **Area:** M2 client.

## Why this epic exists

The client renders `GameState` and has a working Trade UI, but **a human cannot
drive a match by hand**: there is no way to click the board to place/build, roll,
move the robber, or play a dev card. M1's "3–4 players play a Classic match"
E2E was driven by a **scripted test driver**, never the UI — so real interactive
play was never built. This is the gap that makes a live browser match "stick" in
setup (owner saw it 2026-07-14). This epic closes it.

## What already exists (recon 2026-07-14) — the good news

- **Intent dispatch is already generic + wired.** `store.dispatchIntent(intent:
  PlayerIntent)` (`hud/store.ts:186`) forwards ANY `PlayerIntent` to the server
  through the S1.6.5 net layer when connected; the Trade UI proves the whole
  path (compose → dispatch → server `validate`→`reduce`→broadcast → client fold).
  **No protocol/net/server work is needed** — this epic is client input + client
  legality hints only. Server stays the sole authority (it re-validates).
- **Intents exist in core** with the fields the UI must supply: placement/build
  take `vertexId`/`edgeId`; robber takes `tileId`/`victimId`; roll/endTurn/dev
  cards defined (`core/src/types.ts`). Client composes `playerId: myPlayerId`
  (server re-binds to seat — anti-spoof holds, same as Trade UI).
- **`myPlayerId` + turn** are in the store (S1.6.5); inputs enable only on the
  local player's turn.

## What must be BUILT (the lift)

1. **Board hit-testing** — there is NO Pixi interaction on game pieces today
   (only pan/zoom via canvas listeners). Need inverse geometry (pointer →
   nearest `vertexId` / `edgeId` / `tileId`) + a Pixi interaction layer + hover
   affordances. This is the foundational new piece.
2. **Per-phase input wiring** — compose the right intent from a click, gated by
   phase + whose turn it is.
3. **Client-side legal-move hinting** — the client CANNOT call core `validate`
   (needs the server-secret seed), but placement/build legality (distance rule,
   network adjacency, ownership, affordability) IS computable from `GameState`
   with PURE helpers, for highlighting only (server stays authoritative). Reuse/
   extract pure helpers from core where they exist (keep core zero-dep).

## Story breakdown (proposed order — value-first)

- **S2.8.1 — Board hit-testing + interaction layer (foundation).** Pure inverse-
  geometry (`pixelToNearestVertex`/`…Edge`/`…Tile`, snap radius, unit-tested) +
  a Pixi interaction layer emitting `{kind, id}` on click + hover highlight. No
  game logic. Tier 2–3. *Biggest new piece; everything else builds on it.*
- **S2.8.2 — Setup-phase placement (unsticks matches).** In `setup` on my turn:
  highlight legal settlement vertices (distance rule, pure client helper) → click
  → `placeSettlement{vertexId}`; then highlight legal incident road edges → click
  → `placeRoad{edgeId}`; snake-draft order; disabled off-turn. **After this a
  human can complete setup → a real match reaches `main`.** Tier 3.
- **S2.8.3 — Main-phase core loop.** Roll control → `rollDice`; «Завершить ход» →
  `endTurn`; build modes (settlement/road/city) via board clicks with legal-target
  highlight + affordability gating (client hint). Tier 3.
- **S2.8.4 — Robber + 7-discard.** Discard-over-7 picker → `discardResources`;
  move-robber tile-click → `moveRobber{tileId}`; steal-target select →
  `stealResource{victimId}`. Tier 3.
- **S2.8.5 — Dev cards (Предприятия).** Buy (`buyDevCard`) + play each type
  (knight/road-building/YoP/monopoly) with target pickers. Tier 2–3.

**Milestone check:** after S2.8.1+S2.8.2+S2.8.3 a human can play a basic Classic
turn loop end-to-end in the browser; S2.8.4/S2.8.5 complete the ruleset. S2.8.2 is
the single highest-value story (it makes ANY human match playable).

## Progress (2026-07-14)

- **S2.8.1 board hit-testing — MERGED `e343d4a`** (lead-review MERGE, live-QA).
- **S2.8.2 setup placement — MERGED `3f43765`** (lead-review MERGE WITH NITS).
  Live QA confirmed end-to-end: a human clicks to place both settlements + roads,
  legal highlights guide, server rejects illegal (DETACHED_ROAD), bots snake-draft,
  **setup completes and the match reaches the `roll` phase.** (A stuck "Постановка"
  earlier was a leftover quick-match room, NOT a bot-driver bug — fresh server =
  bots place instantly.)
- **S2.8.3a roll + end-turn — MERGED** (lead-review MERGE, 255 client tests green).
  Full browser loop verified: setup complete → `roll` phase, roll button enabled → click roll
  (one roll was a 7 → robber phase `Капер`, the documented S2.8.4 gap; turn-timer
  force-resolved it → `main`) → «Завершить ход» enabled in `main` → click → 3 bots
  play → cycles back to me at `roll` → non-7 roll → straight to `main`. Cosmetic
  follow-up: tide-lot keeps the "Кости не брошены" placeholder after rolling (no
  roll-total surfaced) — story-permitted; surface the total in S2.8.3b or a nit.
- **S2.8.3b main-phase building — MERGED** (lead-review MERGE; 284 client tests;
  legality mirror traced line-by-line vs core, faithful incl. the own-road gate
  setup waives + the enemy-cut). «Строить» opens a settlement/road/city selector
  (affordability + supply gated), a board click dispatches `intent.buildSettlement/
  buildRoad/buildCity` (advisory; server authoritative). Live-QA confirmed the
  selector, cost icons, affordability gating across several hands, and reject UX
  (OCCUPIED/DISTANCE_VIOLATION); a *completed* build was RNG-blocked (no timber+clay
  across ~7 rolls) — not a defect, and the dispatch path is identical to setup
  (proven live). Review nits (follow-ups, non-blocking):
  - **F-buildmode-reset:** clear UI-only `buildMode` on match-join (`applySnapshot`,
    NOT `setGameState` — the latter runs on every event fold and would wipe the
    submode mid-turn). Cosmetic, self-heals on any selector open/close.
  - lore build labels (Стоянка/Гавань/Фарватер) if the owner wants them on
    player-facing build buttons (catalogue currently uses Поселение/Дорога/Город).
  - add a direct unit test for the `touchesOwnNetwork` enemy-cut (mirrored, untested).
- **S2.8.4a robber move + steal — MERGED** (lead-review MERGE WITH NITS; 307 client
  tests; the one real lifecycle nit — stale `robberPendingTile` across robber phases —
  FIXED before merge). A human who rolls a 7 resolves the robber: move prompt → click a
  tile → `intent.moveRobber{tileId}`; if the tile borders an opponent with cards, a
  victim picker (flotilla + hand size + «Отмена») → `intent.moveRobber{tileId,victimId}`.
  Pure `board/robberLegality` (mirrors `validate.ts` `resolveRobberMove`, reuses exported
  `isStealable`) + `hud/useRobberPlacement` + a `RobberPrompt` component; UI-only
  `robberPendingTile` cleared on dispatch/«Отмена»/`applySnapshot`/phase-out. Inert on a
  bot's robber and while `playersToDiscard` non-empty. **Live-QA passed end-to-end:**
  rolled my own 7 → moved robber with no victim AND with a victim (picked Флотилия Моржа →
  steal executed, victim 2→1 card → `main`); UI inert on a bot's robber. Deferred: nit 2
  (victim-button double-click, harmless server-reject).
- **S2.8.4b over-7 discard picker — MERGED `08b1e4f`** (lead-review MERGE WITH NITS; 321
  client tests). A human who rolls (or is hit by) a 7 while holding >7 cards sheds
  `floor(handSize/2)` via a docked `DiscardPanel`: pure advisory gating (`hud/discardActions`
  — `owesDiscard`/`requiredDiscardCount`/`isValidDiscardSelection`, a faithful mirror of core
  `validate.ts:1642-1673` `WRONG_DISCARD_COUNT`/`CANNOT_AFFORD`; `handSize` byte-identical to
  core's) + `hud/DiscardPanel.tsx` (Trade-UI stepper reuse, held-only rows, «Сбросить» enabled
  only at the exact count) → `intent.discard` via `store.dispatchIntent`. **Renders off-turn**
  on `playersToDiscard` membership (the one off-turn-active HUD surface); S2.8.4a's robber-move
  UI stays inert until discards clear, so 4b unblocks it. Selection is component-local `useState`
  (unmounts on the server fold — no optimistic hide; a rejected discard leaves the picker up,
  `justDispatched` 400ms guard self-heals). **Live-QA passed end-to-end:** drove a real 3-bot
  match, banked 13 cards, a 7 → picker "Шторм! Сбросьте 6 карт" (lore + Slavic plural), counter
  0/6, held-only rows, «Сбросить» disabled@0/6 → enabled@6/6 → clicked → server accepted →
  cards **13→7** (−6 exactly) → picker folded. Deferred nits (non-blocking): running-counter
  `aria-live` chattiness; unreachable `discard.prompt.one` plural (owe ⇒ required≥4); hardcoded
  `/2` divisor (documented — latent only if a non-Classic profile reaches a live 7).
- **NEXT: S2.8.5** (dev cards / Предприятия) — buy `buyDevCard` + play each type
  (knight/road-building/YoP/monopoly) with target pickers. The robber flow (S2.8.4a+b) is now
  COMPLETE — a human can play a full Classic turn loop incl. 7s end-to-end in the browser.

## Follow-ups (from S2.8.2 review + live QA — not blocking the epic)

- **F-remount (from review N2, PRE-EXISTING since S2.8.1):** `GameTable`'s
  scene-creation effect keys on `state.buildings`, so the Pixi scene REMOUNTS on
  every placement/build → pan/zoom reset + a flicker between every piece. Harmless
  for setup but bad for a real main-phase game. Fix = update pieces incrementally
  (a scene method that diffs `buildings`), not a full rebuild — a board-rendering
  refactor. Coordinate with S2.7.2 (auto-fit-zoom also touches scene lifecycle).
- **F-notice:** the `NoticeBar` reject (e.g. DETACHED_ROAD) does NOT auto-clear on
  a subsequent successful action (pre-existing S1.6.x behavior). Clear it on the
  next accepted intent, or auto-dismiss after N seconds. Minor UX.

## Cross-cutting constraints (bind every story)

- **DESIGN.md is binding** (§6/§7, a11y §10): legal-target highlights must not be
  hue-only; every new string is `t()` ×ru/uk/en (build-gated); the Chart stays the
  hero. Coordinate with **S2.7.2 auto-fit-zoom** (board-fit) so interaction maps
  correctly under any zoom/pan (hit-testing must invert the live world transform).
- **Client never gains the seed / never calls `validate`.** Legality hints are
  advisory; a click always dispatches an intent and the server is authoritative
  (an illegal click → server reject → existing reject UX).
- **Manual browser QA is mandatory** per every UI surface ([[ui-overlap-manual-qa]]):
  each story eyeballed live (now genuinely drivable once S2.8.2 lands).
- Reuse the Trade UI's `dispatchIntent` seam; don't fork a second dispatch path.

## Open questions for the owner (before build)

1. **Milestone framing** — file this as E2.8 under M2, or is "human-playable
   client" important enough to interleave BEFORE finishing S2.1.7a/b (5–6p board)?
   (Recommend: land S2.8.1+S2.8.2 first — a human-completable setup is worth more
   than the bigger board, and S2.1.7b/S2.7.2 both benefit from real click-testing.)
2. **Scope of "playable" for the first cut** — Classic 4-seat only, or include
   robber/dev-cards in the first push? (Recommend: S2.8.1→2→3 first = a real
   basic loop, then 4/5.)
