// @arch/core — pure deterministic rule engine (zero runtime dependencies).
// ADR-0003: no Date.now(), no Math.random(), no I/O.
// Types and engine implementation land in E0.5.
export const CORE_VERSION = '0.0.1' as const;
