{
  "name": "@arch/server",
  "version": "0.0.1",
  "private": true,
  "description": "Colyseus stateful rooms + Fastify REST (authoritative server)",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsup",
    "test": "vitest run",
    "lint": "eslint src"
  },
  "dependencies": {
    "@arch/core": "workspace:*",
    "@arch/protocol": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8",
    "typescript": "^5"
  }
}
